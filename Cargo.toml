[package]
name = "suggestions_processor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
url = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
